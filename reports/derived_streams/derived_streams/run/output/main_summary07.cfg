filename = "main_summary.lua"
message_matcher = "Uuid > '\096' && Uuid < '\112' && Type == 'telemetry' && Fields[docType] == 'main'"

format		= "redshift.psv"
buffer_path = "../s3output"
buffer_size = 100 * 1024 * 1024
s3_path		= "s3://telemetry-private-analysis-2/derived_streams/data"
